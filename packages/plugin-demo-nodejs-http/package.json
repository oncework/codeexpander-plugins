{
  "name": "@codeexpander/plugin-demo-nodejs-http",
  "version": "0.1.5",
  "private": true,
  "description": "Demo: HTTP Backend (Node.js) - Long-lived backend, HTTP POST. | Demo：HTTP 后端 (Node.js) - 常驻进程，HTTP POST。",
  "scripts": {
    "build": "echo 'No build needed - plain HTML/JS'"
  }
}
