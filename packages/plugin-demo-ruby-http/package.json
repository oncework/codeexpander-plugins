{
  "name": "@codeexpander/plugin-demo-ruby-http",
  "version": "0.1.4",
  "private": true,
  "description": "Demo: HTTP Backend (Ruby) - Long-lived backend, HTTP POST. | Demo：HTTP 后端 (Ruby) - 常驻进程，HTTP POST。",
  "scripts": {
    "build": "echo 'No build needed'"
  }
}
